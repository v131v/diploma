# Final Top-5 Designs

Актуальный финальный список после отдельного post-competition раунда для `design_15`:

1. `design_15` — `94`, `pass`
2. `design_10` — `92`, `pass`
3. `design_01` — `91`, `pass`
4. `design_03` — `90`, `pass`
5. `design_02` — `89`, `pass`

Запасной пул после пересмотра:

- `design_06` — `88`, `pass`
- `design_08` — `88`, `pass`

Общее состояние:

- у всех пяти финалистов нет `critical` и `major`;
- остаются только `minor` замечания;
- `design_15` — единственный финалист, собранный в режиме `full-corpus synthesis`;
- у `design_15` финальный review-chain заканчивается файлом `design_15_round3.md`, у остальных финалистов используются их pass-review второго раунда.
